# Murder Bot

React control surface for the Keep backend.

```sh
npm install && npm run dev
```

The frontend expects the backend at `http://127.0.0.1:8000`. Override it with `VITE_API_BASE_URL` when needed:

```sh
VITE_API_BASE_URL=http://127.0.0.1:8000 npm run dev
```
